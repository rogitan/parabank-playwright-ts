name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      matrix:
        project: [UI]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run Playwright tests
        run: npx playwright test --project=${{ matrix.project }}
        env:
          BASE_URL: ${{ secrets.BASE_URL || 'https://parabank.parasoft.com' }}

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-${{ matrix.project }}
          path: reports/html/
          retention-days: 14

      - name: Upload JUnit results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: junit-results-${{ matrix.project }}
          path: reports/junit/results.xml
          retention-days: 14

      - name: Upload Allure results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-${{ matrix.project }}
          path: allure-results/
          retention-days: 7

      - name: Upload test traces & screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-failure-artifacts-${{ matrix.project }}
          path: test-results/
          retention-days: 7

  send-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: e2e-tests
    if: always()

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Download all JUnit results
        uses: actions/download-artifact@v4
        with:
          pattern: junit-results-*
          path: junit-results
          merge-multiple: true

      - name: Generate test summary
        id: summary
        run: node .github/workflows/generate-report.js
        env:
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Check SMTP secrets
        id: smtp_check
        run: |
          if [ -n "${{ secrets.MAIL_USERNAME }}" ] && [ -n "${{ secrets.MAIL_PASSWORD }}" ]; then
            echo "configured=true" >> $GITHUB_OUTPUT
          else
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "::warning::SMTP not configured. Set MAIL_USERNAME, MAIL_PASSWORD, and MAIL_TO secrets to receive email reports."
          fi

      - name: Send email report
        if: steps.smtp_check.outputs.configured == 'true'
        uses: dawidd6/action-send-mail@v4
        with:
          server_address: ${{ secrets.MAIL_SERVER || 'smtp.gmail.com' }}
          server_port: ${{ secrets.MAIL_PORT || '587' }}
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: |
            [Parabank Tests] ${{ steps.summary.outputs.status }} — ${{ steps.summary.outputs.total }} tests, ${{ steps.summary.outputs.passed }} passed, ${{ steps.summary.outputs.failed }} failed
          to: ${{ secrets.MAIL_TO }}
          from: ${{ secrets.MAIL_FROM || secrets.MAIL_USERNAME }}
          html_body: file://report-body.html
          ignore_cert: ${{ secrets.MAIL_IGNORE_CERT || 'false' }}
